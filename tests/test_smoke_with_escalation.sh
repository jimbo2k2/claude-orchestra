#!/bin/bash
# Smoke test for the with-escalation fixture. Invokes real Claude — opt-in only.
#
# To actually run: ./bin/orchestra test with-escalation
# (Takes 5-15 minutes; burns API tokens; not part of the auto-run suite.)
echo "  SKIP: opt-in smoke test (run manually: ./bin/orchestra test with-escalation)"
exit 0
