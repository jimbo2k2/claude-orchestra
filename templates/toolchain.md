# Toolchain — Stack Reference

Task-agnostic stack reference for Orchestra sessions. Customise for your project.

## Prerequisites

<!-- List system-level tools required. Orchestra validates these at preflight. -->
- node (>=18)

## Build & Dev

```bash
# Replace with your project's commands
npm install
npm run dev
npm run build
```

## Testing

```bash
# Replace with your project's test commands
npm test
```

## Type Checking

```bash
# Replace with your project's type checker
npx tsc --noEmit
```
