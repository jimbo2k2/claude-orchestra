# .orchestra/config — Orchestra v3 configuration
# All settings for an Orchestra run. No env var overrides. No CLI args.

# ─── Task Queue ──────────────────────────────────────────────────────────────
# Comma-separated T-numbers for this run. Orchestra reads these from TODO.md,
# ignores the status field, and sequences them intelligently.
TASKS=

# ─── Session Limits ──────────────────────────────────────────────────────────
MAX_SESSIONS=10
MAX_CONSECUTIVE_CRASHES=3

# ─── Quota Pacing ────────────────────────────────────────────────────────────
QUOTA_PACING=true
QUOTA_THRESHOLD=80
QUOTA_POLL_INTERVAL=120

# ─── Cooldowns (seconds) ─────────────────────────────────────────────────────
COOLDOWN_SECONDS=15
CRASH_COOLDOWN_SECONDS=30

# ─── Model ───────────────────────────────────────────────────────────────────
# Default for all sessions. Subtasks can recommend lower, not higher.
MODEL=opus
EFFORT=high

# ─── Governance Paths (relative to project root) ─────────────────────────────
TODO_FILE=TODO/TODO.md
DECISIONS_FILE=Decisions/DECISIONS.md
CHANGELOG_FILE=Changelog/CHANGELOG.md

# ─── Protocol ────────────────────────────────────────────────────────────────
DEVELOPMENT_PROTOCOL=DEVELOPMENT-PROTOCOL.md

# ─── Toolchain ───────────────────────────────────────────────────────────────
TOOLCHAIN_FILE=.orchestra/toolchain.md

# ─── Worktree ────────────────────────────────────────────────────────────────
# Orchestra runs in a git worktree so your main working tree stays untouched.
WORKTREE_BASE=/tmp/orchestra

# ─── Tmux ────────────────────────────────────────────────────────────────────
# Orchestra always runs in a tmux session. Name is checked for conflicts.
TMUX_SESSION=orchestra
